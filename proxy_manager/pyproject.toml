[project]
name = "proxy-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "redis>=5.0.3",
    "httpx>=0.28.1",
    "pyyaml>=6.0.1"
]
